{
  "name": "astro-component-demo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build:sass": "sass src/scss:dist/css",
    "watch:sass": "sass --watch src/scss:public/css",
    "start": "npm-run-all --parallel dev watch:sass",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^0.26.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.52.1"
  },
  "dependencies": {
    "codemirror": "^5.65.4"
  }
}
